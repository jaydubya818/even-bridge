# App configuration
PORT=3000

# Meeting-assistant overlay integration (dev only)
DEV_MEETING_ASSISTANT_OVERLAY=true
MEETING_ASSISTANT_OVERLAY_URL=http://localhost:3000/even-hub/overlay

# Messenger credentials and API keys are now managed
# through the Settings UI in the browser.
# Open the app and click the gear icon to configure.
